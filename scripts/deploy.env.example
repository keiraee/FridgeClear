# Copy to deploy.env and fill in (deploy.env is gitignored).
DEPLOY_SERVER=root@8.137.126.178
DEPLOY_REMOTE_DIR=/opt/fridgeclear

# Optional: only if you use password login and have sshpass installed (brew install hudochenkov/sshpass/sshpass).
# Prefer: ssh-copy-id root@8.137.126.178  then leave this empty.
DEPLOY_SSH_PASSWORD=
