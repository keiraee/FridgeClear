# Alibaba Cloud RDS / local database connection
DB_HOST=rm-2vc0m0i62377rv312jo.mysql.cn-chengdu.rds.aliyuncs.com
DB_PORT=3306
DB_NAME=fridge_clear
DB_USERNAME=fridge_clear
DB_PASSWORD=replace_with_your_password
DB_USE_SSL=true

# Only required when starting the local MySQL Docker container.
MYSQL_ROOT_PASSWORD=replace_with_root_password

# AES-256 Base64 key for encrypting AI provider API keys. Generate with:
# openssl rand -base64 32
AI_PROVIDER_ENCRYPTION_KEY=replace_with_base64_32_byte_key

# JWT signing key. Generate with:
# openssl rand -base64 32
AUTH_JWT_SECRET=replace_with_base64_32_byte_key
AUTH_JWT_EXPIRATION_SECONDS=7200

# 全局平台 AI 配置（可选种子）：仅首次启动且 system_ai_config 为空时写入，
# 之后由管理端页面（/admin/ai-config）维护。
AI_PLATFORM_PROVIDER_NAME=platform_default
AI_PLATFORM_PROTOCOL=OPENAI_CHAT
AI_PLATFORM_BASE_URL=https://api.openai.com/v1
AI_PLATFORM_MODEL_NAME=gpt-4o-mini
AI_PLATFORM_API_KEY=replace_with_platform_api_key

# 是否允许用户自配 AI Provider（默认关闭，仅平台全局配置）
AI_BYOK_ENABLED=false

# 首个管理员引导（可选）：启动时将 ADMIN_BOOTSTRAP_EMAIL 邮箱用户提升为 ADMIN，不存在则创建。
ADMIN_BOOTSTRAP_EMAIL=admin@example.com
ADMIN_BOOTSTRAP_PASSWORD=replace_with_admin_password
